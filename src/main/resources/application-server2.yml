server:
  port: 8762

spring:
  application:
    name: eureka2

eureka:
  instance:
    hostname: eureka2
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka1:8761/eureka/

